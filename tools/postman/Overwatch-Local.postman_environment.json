{
  "name": "Overwatch \u2014 Local",
  "values": [
    {
      "key": "baseUrl",
      "value": "http://localhost:8080",
      "enabled": true,
      "description": "Fraud API. Change if preflight moved it \u2014 check `make urls`."
    },
    {
      "key": "simulatorUrl",
      "value": "http://localhost:8081",
      "enabled": true,
      "description": "Only needed to hit the simulator directly, which takes `make up-tools`. The collection does not use it: fraud-api proxies the whole control surface at {{baseUrl}}/api/simulator."
    },
    {
      "key": "alertId",
      "value": "",
      "enabled": true,
      "description": "Filled in automatically by the List alerts request."
    },
    {
      "key": "ruleId",
      "value": "1",
      "enabled": true,
      "description": "Any seeded rule id; 1 through 7 exist after V2 runs."
    }
  ],
  "_postman_variable_scope": "environment"
}
